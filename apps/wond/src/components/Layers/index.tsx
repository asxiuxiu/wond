import './index.scss';

const Layers = () => {
  return (
    <div className="layers">
      <div className="layers-header">
        <div className="layers-header-title">Layers</div>
      </div>
    </div>
  );
};

export default Layers;
